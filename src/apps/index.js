// main.jsx o App.jsx
import {
  // Kernel
  WindowManagerProvider,
  useWindowManager,
  // Arranque
  BootstrapProvider,
  BootLoaderProvider,
  SafeBootProvider,
  // UI del sistema
  Desktop,
  MenuBar,
  Dock,
  Launchpad,
  Spotlight,
  // Bloqueo
  LockScreenProvider,
  LockScreenView,
  // Apps
  Finder,
  Terminal,
  Notes,
  Settings,
  About,
  // Extras
  ToastProvider,
  UpdaterProvider,
} from "rainOS";
